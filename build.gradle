plugins {
    id 'java'
    id 'idea'
}

repositories {
    mavenCentral()
}

ext {
    junitVersion = '4.12'
}

dependencies {
    testCompile("junit:junit:${junitVersion}")
}

test {
    useJUnitPlatform()
    testLogging {
        events "passed", "skipped", "failed"
    }
}